{
  "name": "miway-transit",
  "version": "0.0.1",
  "private": true,
  "homepage": "https://dmatthew2024.github.io/miway-transit",  // URL for GitHub Pages hosting
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",  // Build before deployment
    "deploy": "gh-pages -d dist"   // Deploy the `dist` folder to `gh-pages` branch
  },
  "dependencies": {
    "axios": "^0.21.1",
    "leaflet": "^1.7.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-leaflet": "^3.2.0"
  },
  "devDependencies": {
    "vite": "^2.4.4",
    "@vitejs/plugin-react": "^1.0.0",
    "gh-pages": "^3.2.3"  // Required for GitHub Pages deployment
  }
}
